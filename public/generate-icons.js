// This script is a helper note — actual PNG icons are generated via SVG at build time
// vite-plugin-pwa handles icon generation from the SVG automatically
// For manual PNG generation: use https://realfavicongenerator.net with icon-prime.svg
console.log('Icons are served as SVG — supported by all modern browsers and Android Chrome');
